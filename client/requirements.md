## Packages
framer-motion | Smooth page transitions and UI animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
date-fns | Date formatting for chat messages and lists

## Notes
Voice integration uses provided hooks in @/replit_integrations/audio/
Images are located in /client/public/images/ (served at /images/)
AudioWorklet needs to be available in public folder (already handled by integration tool)
